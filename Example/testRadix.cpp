#include <bits/stdc++.h>
#include<iostream>

using namespace std;

int main()
{
    // int array[5] = {20, 56, 6, 78, 40};
    // int output[5];

    // for (int i = size - 1; i >= 0; i--)
    // {
    //     output[count[(array[i] / place) % 10] - 1] = array[i];
    //     count[(array[i] / place) % 10]--;
    // }

    int n = ((56 / 1) % 10) - 1;

    cout << n;

    system("pause");
    return 0;
}